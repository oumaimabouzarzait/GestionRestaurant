@page
@model ProjetASI.Pages.Barmans.IndexModel

@{
    ViewData["Title"] = "Les barmans";
}

<h1>Les barmans</h1>

<p>
    <a asp-page="Create">Ajouter un barman</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Barman[0].Nom)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Barman[0].Prenom)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Barman) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Prenom)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Éditer</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Détails</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Supprimer</a>
            </td>
        </tr>
}
    </tbody>
</table>
